'use strict';
function evaluate(input={},context={}){const errors=[]; if(String(input.ownerId||'')!==context.actor)errors.push('ownerId must match the signed actor'); if(input.rightsConfirmed!==true)errors.push('media rights confirmation required'); if(input.subjectConsent!==true)errors.push('subject consent required'); if(input.moderationApproved!==true)errors.push('moderation approval required'); const duration=Number(input.durationSeconds); if(!Number.isFinite(duration)||duration<=0)errors.push('positive durationSeconds required'); const signals=Array.isArray(input.signals)?input.signals:[]; const candidates=signals.map((s,i)=>{const start=Number(s.start);const end=Number(s.end);const speech=Number(s.speechConfidence);const motion=Number(s.motionScore); if(!Number.isFinite(start)||!Number.isFinite(end)||start<0||end<=start||end>duration)errors.push(`signals[${i}] timing invalid`); if(!Number.isFinite(speech)||speech<0||speech>1||!Number.isFinite(motion)||motion<0||motion>1)errors.push(`signals[${i}] scores invalid`); return {start,end,score:Number((speech*0.7+motion*0.3).toFixed(4)),sourceSignalId:String(s.id||i)};}).filter(x=>x.score>=0.65).sort((a,b)=>b.score-a.score||a.start-b.start); if(!String(input.renderProfileVersion||'').trim())errors.push('renderProfileVersion required'); if(!Array.isArray(input.languages)||!input.languages.length)errors.push('at least one caption language required'); return {errors,result:{schemaVersion:1,assetRef:String(input.assetRef||''),timelineVersion:1,candidateHighlights:candidates,renderProfileVersion:String(input.renderProfileVersion||''),languages:[...(input.languages||[])].map(String).sort(),publishState:'review_required',watermarkRequired:input.watermarkRequired!==false,disclosureRequired:true,humanApprovalRequired:true},assumptions:['Time-coded signals are supplied by an approved media/transcription provider.'],uncertainty:{highlightSelection:'heuristic_candidates_only',exportDeterministic:true}};}
module.exports={evaluate};

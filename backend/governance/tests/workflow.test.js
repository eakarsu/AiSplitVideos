'use strict'; const test=require('node:test');const assert=require('node:assert/strict');const {evaluate}=require('../domain');const ctx={actor:'editor-1'};const base={ownerId:'editor-1',rightsConfirmed:true,subjectConsent:true,moderationApproved:true,durationSeconds:60,assetRef:'asset:1',renderProfileVersion:'vertical-v2',languages:['es','en'],signals:[{id:'s1',start:1,end:8,speechConfidence:.9,motionScore:.8},{id:'s2',start:10,end:20,speechConfidence:.2,motionScore:.2}]};
test('highlight detection produces deterministic review candidates',()=>{const out=evaluate(base,ctx);assert.deepEqual(out,evaluate(base,ctx));assert.equal(out.result.candidateHighlights.length,1);assert.equal(out.result.publishState,'review_required');});test('rights, consent, moderation and timing fail closed',()=>{const out=evaluate({...base,rightsConfirmed:false,subjectConsent:false,moderationApproved:false,signals:[{start:-1,end:80,speechConfidence:2,motionScore:-1}]},ctx);assert.ok(out.errors.length>=5);});
